[project]
name = "shared"
version = "0.1.0"
description = "Modelos y utilidades comunes para el sistema de scraping"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"