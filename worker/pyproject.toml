[project]
name = "worker"
version = "0.1.0"
description = "Encargado de scrapear las web que el producer indica"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "shared",
]

[tool.uv.sources]
shared = { path = "../shared" }